# PlantOS Specifications

Specification documents are versioned under this directory. Each version lives in its own folder.

Current contents:
- `v1.0/PLANT-OS_SPEC.md`: Draft of the 1.0 specification describing architecture, workflows, and roadmap.

Add a `CHANGELOG.md` next to each version file when releasing a new milestone.
